{% extends 'base.html.twig' %}

{% form_theme formPhoto 'bootstrap_5_layout.html.twig' %}

{% block title %} - Ajouter une photo{% endblock %}

{% block body %}
  <section class="form">
    <h2>Une petite illustration ?</h2>

    {{ form_start(formPhoto) }}
        {{ form_row(formPhoto.imageFile) }}
        <button type="submit" class="btn btn-primary">
          Sauvegarder 
        </button>
    {{ form_end(formPhoto) }}
  </section>
{% endblock %}