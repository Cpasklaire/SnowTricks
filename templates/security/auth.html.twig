{% extends 'base.html.twig' %}

{% block title %}Hello SecurityController!{% endblock %}

{% block body %}
    <h2>Inscription</h2>
      {{ form_start(form) }}

        {{ form_row(form.Pseudo) }}
        {{ form_row(form.email) }}
        {{ form_row(form.password, {'attr': {'type': 'password'}}) }}
        {{ form_row(form.confirmPassword, {'attr': {'type': 'password'}} ) }}
        {{ form_row(form.avatar) }}

    <button type="submit" class="btn btn-primary">
          Créer
    </button>

  {{ form_end(form) }}
{% endblock %}
